# GitHub Security Scanner — Dependencias
# Instalar con: pip install -r requirements.txt
# NOTA: Para desarrollo, usar: pip install -e ".[dev,security]"

# HTTP client
requests>=2.31.0

# Variables de entorno
python-dotenv>=1.2.2

# Reglas personalizadas en YAML
PyYAML>=6.0.1

# CLI coloreado (opcional, para mejores logs)
colorama>=0.4.6; sys_platform == "win32"

# Seguridad (requerido para vulnerabilidades conocidas)
urllib3>=2.6.3
